services:
  frontend:
    env_file:
      - .env
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    expose:
      - "8000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:alpine
